# Socrates Transcript Extractor

WXT browser extension for extracting readable YouTube transcripts.

## Commands

- `npm run compile` checks TypeScript.
- `npm run build` builds the Chrome MV3 extension into `.output/chrome-mv3`.
